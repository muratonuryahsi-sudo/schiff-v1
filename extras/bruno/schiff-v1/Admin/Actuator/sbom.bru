meta {
  name: sbom
  type: http
  seq: 7
}

get {
  url: {{actuator_url}}/sbom
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

script:post-response {
  test('Statuscode 200', () => {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
