meta {
  name: Shutdown
  type: http
  seq: 3
}

post {
  url: {{actuator_url}}/shutdown
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.message: matches ^Shutting down
}

settings {
  encodeUrl: true
  timeout: 0
}
